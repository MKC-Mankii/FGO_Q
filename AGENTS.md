# Project Rules & Preferences

## 代理行为与验证规范 (Agent Guidelines)

- **允许调用，但严禁擅自执行复杂任务**：
  可以调用 `browser_subagent`（如响应用户打开网页需求），但**未经用户明确要求，严禁擅自给子代理下发任何点击、巡检、DOM 检查或交互测试等复杂任务**。
- **展示场景打开即停**：
  用户仅要求打开或查看页面时，子代理任务**仅限导航到目标 URL**，页面载入后立即静止退出，将所有视觉与操作控制权完整交由用户。
