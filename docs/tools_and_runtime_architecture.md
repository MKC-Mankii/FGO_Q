# 运行工具体系与协同架构说明

本文档详细说明 FGO_Q 自动化项目中涉及的各项开发、调试与运行工具，解析 **PC 按键精灵手机助手**、**安卓模拟器** 以及 **模拟器中的按键精灵手机版** 之间的底层机制、打包交互逻辑、典型陷阱以及 V4 架构的解耦设计。

---

## 1. 工具体系全景与拓扑关系

在整个自动化体系中，共有四个核心实体协同工作：

```text
┌────────────────────────────────────────────────────────────────────────┐
│                          Windows 宿主机 (PC 端)                         │
│                                                                        │
│   ┌───────────────────────────┐      ┌─────────────────────────────┐   │
│   │   FGO 配置编辑器 (Web/桌面) │      │      PC 按键精灵手机助手      │   │
│   │   • 端口 8098 (V4) / 8099 (V3) │      │ • 工程管理与脚本编辑          │   │
│   │   • 战术 DSL 编排与参数调整     │      │ • 资源打包 (.mq + .atc)     │   │
│   │   • adb_sync 自动直推模块      │      │ • 传统 F5 调试下发宿主       │   │
│   └─────────────┬─────────────┘      └──────────────┬──────────────┘   │
│                 │                                   │                  │
│                 │ (1) 自动同步 PC 助手脚本           │                  │
│                 └───────────────────────────────────┤                  │
│                                                     │                  │
│                 ┌───────────────────────────────────┘                  │
│                 │ (2) ADB 调试桥通道 (端口: 5555 / 16384 等)           │
│                 ▼                                                      │
├────────────────────────────────────────────────────────────────────────┤
│                           安卓模拟器 (MuMu 宿主)                        │
│                                                                        │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │ Android 共享存储区 (/sdcard/)                                   │   │
│   │   ├── /sdcard/FGO_Q/                   ◄── [V4 规范化独立总线]  │   │
│   │   │     ├── battle_v4_config.mq            (免复制直推目标)     │   │
│   │   │     └── cmd.txt / status.txt           (阶段二指令总线)     │   │
│   │   └── /sdcard/MobileAnJian/Script/     ◄── [按键精灵内部工作区] │   │
│   │         ├── fgo_battle_v4_runner(...).mq   (Runner 执行引擎)   │   │
│   │         ├── fgo_battle_v4_runner(...).atc  (图色特征/紫猫插件) │   │
│   │         └── fgo_battle_v4_config(...).mq   (工程历史配置)       │   │
│   └────────────────────────────────────────────────────────────────┘   │
│                                 ▲                                      │
│                                 │ 读写交互                             │
│   ┌─────────────────────────────┴──────────────────────────────────┐   │
│   │ 模拟器内部运行应用                                              │   │
│   │   • 按键精灵手机版 (App + Root 守护进程: 负责找图、截屏、点击)   │   │
│   │   • Fate/Grand Order 游戏客户端                                 │   │
│   └────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 2. PC 按键精灵手机助手

### 2.1 定位与职责
- **官方 Windows 客户端**：按键精灵官方提供的 Android 移动端脚本集成开发环境（IDE）。
- **工程资产管理**：管理脚本源码、界面配置（`.prop` / `.uis`）以及图色资源附件（`.atc`）。
- **调试连接桥梁**：负责与模拟器（或真机）建立连接，将脚本分发下发至移动端并捕获调试日志。

### 2.2 本地工作空间文件结构
PC 手机助手的脚本工作目录位于 Windows 本地路径：
`D:\ProgramData\按键精灵\按键精灵手机助手\Script\`

每个脚本工程均以其工程名称与内部生成的 GUID 组合命名：
- **`*.mq`**：按键精灵脚本源码（采用 Windows **GBK** 编码，CRLF 换行）。
- **`*.atc`**：附件资源压缩包（包含 `Attachment:` 找图用的全部小图素材，以及紫猫插件 `zm.luae`）。
- **`*.lc`**：按键精灵手机助手编译生成的 Lua Bytecode 缓存。
- **`*.prop` / `*.uis`**：工程属性元数据与移动端配置界面描述。

### 2.3 调试下发与打包机制 (核心原理)
当开发者在 PC 助手界面中点击 **「调试」** 按钮（或按快捷键 **`F5`**）时，PC 助手会在后台执行以下标准动作：
1. **本地打包**：将当前编辑器内的脚本源码保存，并与工程所属的 `.atc` 附件包一起打包；
2. **ADB 推送传输**：通过 ADB 连接通道，将生成的 `*.mq` 与 `*.atc` 强行推送（`adb push`）至模拟器的 `/sdcard/MobileAnJian/Script/` 目录；
3. **唤起执行信号**：通过 Socket / Intent 向模拟器内运行的按键精灵守护进程发送“开始调试”指令；
4. **日志双向回传**：移动端脚本通过 `TracePrint` 打印的日志，会通过网络管道实时回传并渲染在 PC 助手的底部调试控制台中；
5. **结束退出**：脚本执行完毕或手动停止后，PC 助手控制台提示 `已退出调试模式`。

### 2.4 典型痛点与覆盖冲突 (Gotchas)
> [!WARNING]
> **PC 助手的“单向覆盖陷阱”**：
> PC 手机助手始终以 Windows 本地 `D:\ProgramData\按键精灵\...\Script\` 下的文件为**绝对权威源（Single Source of Truth）**。
> 
> 如果开发者或外部程序仅仅修改了模拟器端 `/sdcard/MobileAnJian/Script/` 中的配置或脚本，只要有人在 PC 助手里按下了 `F5`（或点击了保存/调试），PC 助手就会**毫不犹豫地将本地旧缓存打包并覆盖模拟器**，导致模拟器中的新配置瞬间被“冲回旧版”！

**FGO_Q 的应对设计**：
为了解决这一致命冲突，在 V4 版本的配置编辑器中：
- 每次在 Web 端点击保存或按 `Ctrl+S` 时，不仅直推模拟器，还会**同步覆写本地 PC 手机助手的对应 `.mq` 文件**；
- 从而无论后续是否误按 F5，本地与模拟器端的数据永远保持严格一致。

---

## 3. 安卓模拟器 (Android Emulator)

### 3.1 定位与环境
- **虚拟化运行容器**：本项目主要基于 **MuMu 模拟器（MuMu 12 / MuMu 6）**，运行 Android 操作系统与游戏本体（`com.bilibili.fatego`）。
- **ADB 宿主接口**：模拟器在宿主机暴露 ADB 调试服务端口（常见如 `127.0.0.1:5555`，设备标识形如 `emulator-5554`），自带独立 ADB 二进制程序（如 `D:\Program Files\Netease\MuMu\nx_main\adb.exe`）。

### 3.2 存储空间划分与权限
1. **公共存储空间 (`/sdcard/`，即 `/storage/emulated/0/`)**：
   - 全局可读写空间，无需复杂 Linux 提权即可通过 ADB 直接访问。
2. **按键精灵内部空间 (`/sdcard/MobileAnJian/`)**：
   - 按键精灵手机版及其 PC 助手专属空间，结构不可控，升级、清空缓存或重新打包容易受到干扰。
3. **FGO_Q 规范化总线通信目录 (`/sdcard/FGO_Q/`)**：
   - 本项目专门开辟的独立总线目录；
   - 彻底脱离按键精灵的内部黑盒管理；
   - 作为配置下发（`battle_v4_config.mq`）与指令控制（`cmd.txt`、`status.txt`）的标准总线接口。

---

## 4. 模拟器中的按键精灵 (Mobile AnJian)

### 4.1 定位与系统结构
- **执行宿主 App**：安装在模拟器中的 Android 应用（`com.cyjh.mobileanjian`）。
- **底层 Root 守护进程**：按键精灵启动后会生成具有 Root 权限的守护子进程（如 `com.cxrjn.apjzj:knjpj`），获取系统底层的截屏快照、虚拟触控（Input Tap/Swipe）及物理按键注入权限。

### 4.2 两种运行方式对比

| 维度 | PC 助手调试运行模式 | 模拟器悬浮窗脱机运行模式 |
| :--- | :--- | :--- |
| **触发载体** | PC 端按键精灵手机助手界面（F5） | 模拟器屏幕右侧悬浮窗（悬浮小狮子图标） |
| **PC 助手依赖** | 强依赖（必须打开 PC 助手且保持连接） | 完全不依赖（PC 助手可直接关闭） |
| **脚本来源** | PC 助手每次点击调试时实时打包下发 | 读取模拟器内部 `/sdcard/MobileAnJian/Script/` 最后一次留存的工程 |
| **日志查看** | PC 助手底部控制台实时输出 | 悬浮窗简要提示或仅能在模拟器内部浮窗查看 |
| **日常使用评价** | 适合深度开发找图与断点调试 | 适合日常挂机战斗，但切换配置仍受限于旧版文件扫描 |

### 4.3 核心依赖插件 (紫猫插件 `zm.lua`)
Runner 核心引擎重度依赖打包进 `.atc` 的紫猫插件（`zm.lua` / `zm.luae`），其主要赋能以下能力：
- **`zm.FindPic`**：高性能模糊找图、偏色匹配与多图并发检索；
- **`zm.DirScan`**：目录动态文件扫描与通配符过滤；
- **`zm.FileRead`**：大文本快速读取与配置字符串解析。

### 4.4 移动端运行时的技术边界与限制

在按键精灵移动端开发与架构设计中，存在以下关键技术约束，直接决定了当前的代码加载与文件总线方案：
1. **模块引用限制（`Import` 局限）**：
   - 按键精灵手机助手的 `Import` 语法仅支持引入打包在附件中的官方插件（如 `zm.luae`），无法直接通过 `Import` 动态加载或执行外部独立的 `.q` / `.mq` 逻辑脚本代码。
2. **附件包隔离限制（`.atc` 无法作为普通文件读取）**：
   - `.atc` 附件包是经加密压缩的资源包，内置函数 `File.Read` 或操作系统文件 API 只能读写 `/sdcard/` 等标准文件系统路径，无法把打包在 `.atc` 里的文本附件直接当普通文本文件按路径读出。
   - **架构对策**：因此配置无法直接内嵌在 `.atc` 中热更新，必须以外部独立文本文件（如 `/sdcard/FGO_Q/battle_v4_config.mq`）形式存放在模拟器标准存储区，并在 Runner 启动时通过 `zm.FileRead` 动态载入解析。

---

## 5. 从 V3 到 V4 的演进对比

### 5.1 V3 的旧有模式（纯人工“搬砖”式链路）
```text
Web 编辑器修改配置 ──(Ctrl+S)──> 本地 Q/battle_v3_config.q
                                         │
                                         ▼ (人工全选、复制)
PC 手机助手编辑器  ◄─────────────────────┘
       │
       ▼ (人工按 F5 调试)
PC 助手打包并覆盖推送 ───────────────> 模拟器 /sdcard/MobileAnJian/Script/
                                         │
                                         ▼ (Runner 通过 DirScan 扫描加载)
                                  模拟器内按键精灵开打
```
- **核心缺陷**：每次调队伍或改技能都必须重复“复制、切窗口、粘贴、F5”；一旦漏贴导致配置不一致，甚至可能发生旧版配置误覆盖。

### 5.2 V4 的全新模式（独立总线 + ADB 毫秒直推 + 离线自愈）
```text
Web 编辑器修改配置 ──(Ctrl+S)──> 本地 Q/battle_v4_config.q (含自动快照)
        │
        ├──(自动静默覆盖)───────> 本地 PC 助手目录 (防 F5 误冲)
        │
        └──(ADB 毫秒级直推)─────> 模拟器 /sdcard/FGO_Q/battle_v4_config.mq
                                         │
                                         ▼ (Runner 优先直读 /sdcard/FGO_Q/)
                                  模拟器内即刻生效！
```
- **核心突破**：
  1. **零人工复制**：配置修改保存后 200ms 内直抵模拟器；
  2. **总线解耦**：`/sdcard/FGO_Q/` 不受 PC 助手调试动作污染；
  3. **离线自愈**：前端 10 秒轻量心跳监测模拟器状态，断线期间修改配置在模拟器开机重连后**全自动静默补推**；
  4. **为阶段二直控铺平道路**：未来通过在 `/sdcard/FGO_Q/` 写入 `cmd.txt: start` 与 `cmd.txt: stop`，将彻底免除模拟器悬浮窗或 PC 助手的任何手动启动交互。
